import { AlumnosFilterPipe } from './alumnos-filter.pipe';

describe('AlumnosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumnosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
